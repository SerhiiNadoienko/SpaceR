"use client";

export const AddToCard = () => {
  return (
    <button
      style={{ cursor: "pointer" }}
      onClick={() => console.log("add to card")}
    >
      add to card
    </button>
  );
};
